{
  "name": "deep-research-argentina-fullstack",
  "version": "1.0.0",
  "description": "Full stack deep research application",
  "scripts": {
    "dev:backend": "cd backend && uv run uvicorn server:app --reload --port 8000",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
