{
  "name": "deep-research-argentina-fullstack",
  "version": "1.0.0",
  "description": "Full stack deep research application",
  "scripts": {
    "dev:orchestrator": "cd agents/orchestrator && uv run uvicorn server:app --reload --port 8000",
    "dev:eventos": "cd agents/agent_eventos && uv run uvicorn server:app --reload --port 8001",
    "dev:explainer": "cd agents/agent_explainer && uv run uvicorn server:app --reload --port 8002",
    "dev:frontend": "cd frontend && npm run dev",
    "dev": "concurrently \"npm run dev:orchestrator\" \"npm run dev:eventos\" \"npm run dev:explainer\" \"npm run dev:frontend\""
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}